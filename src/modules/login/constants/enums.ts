export enum SocialProviderEnum {
  Facebook = 1,
  LinkedIn,
  Google,
};

export enum ModalContentEnum {
  Login = 1,
  SignUp,
  Restore,
  SignUpVerify,
  RestoreVerify,
};

export enum VerifyTypeEnum {
  SignUp = 1,
  Restore,
};